# ============================================
# OpenAI API Configuration
# ============================================
# Required: Your OpenAI API key for AI agent functionality
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
# Optional: If not set, the application will use SQLite (sqlite:///rss_articles.db)
# If all PostgreSQL variables are set, PostgreSQL will be used instead

POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=rss_articles

# ============================================
# Email Configuration
# ============================================
# Required: Gmail account settings for sending daily newsletters
# For Gmail, you need to create an "App Password" instead of using your regular password
# Instructions: https://support.google.com/accounts/answer/185833

EMAIL_SUBJECT=Daily News Brief
EMAIL_SENDER=your_email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password_here

# ============================================
# Language Configuration
# ============================================
# Language for article summaries and translations
# Options: "ES" (Spanish) or "ENG" (English)
# Default: "ES"
LANGUAGE=ES

# ============================================
# AI Agent Model Configuration
# ============================================
# OpenAI model to use for article processing and summarization
# Recommended: "gpt-4o-mini" (cost-effective) or "gpt-4o" (higher quality)
# Default: "gpt-4o-mini"
AGENT_MODEL=gpt-4o-mini

# ============================================
# Web Search Model Configuration
# ============================================
# OpenAI model to use for web search functionality (when article content is missing)
# Recommended: "gpt-4o" or "o1-preview" (if available)
# Optional: Only needed if using web search feature
WEB_SEARCH_MODEL=gpt-4o

# ============================================
# Article Processing Configuration
# ============================================
# Maximum number of top-ranked articles to process and include in newsletter
# Default: 10
TOP_RANK=10

# Maximum number of articles to fetch per RSS feed
# Default: 10
MAX_ARTICLES=10
